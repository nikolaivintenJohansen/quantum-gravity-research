# Toy models

Each selected model receives a stable directory containing `code/`, `notebooks/`, `figures/`, `results/`, and an append-only `log.md`. Canonical executable Python and Wolfram files remain under the repository-level language directories and are referenced from the model directory.
